# Detection (index)

Detection engineering is a first-class objective. For each attack path, this directory will
catalog telemetry sources (web/DNS/auth/Windows event logs, network sensor) and candidate
detections (e.g., Sigma/Suricata), with validation and tuning notes. Implemented in later phases.
