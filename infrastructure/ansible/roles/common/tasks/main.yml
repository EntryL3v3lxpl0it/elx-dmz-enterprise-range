---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
  become: true

- name: Install baseline packages
  ansible.builtin.apt:
    name:
      - curl
      - git
      - ufw
      - ca-certificates
      - gnupg
    state: present
  become: true
